# ds
DwimScript interpeter
Matt Hansen - mghansen@gmail.com

This project aims to define a simple programming language, and implement a parser (dsp) and interpreter for it (dsi). This will lead into a bytecode compiler (dsc) for the language that will run on a custom virtual machine (dsx).
