# ds
DwimScript interpeter
Matt Hansen - mghansen@gmail.com

This project aims to define a simple programming language (see ds_bnf.txt), and implement a parser (dsp) and interpreter (dsi) for it. This will lead into a compiler (dsc) for the language that will create bytecode executables that will run on a custom virtual machine (dsx).
