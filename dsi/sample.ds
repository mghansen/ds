func main()
	c = counting()
	//f = factorial(5)
end

func early()
	return(11)
	return(10)
end

func factorial(f)
	q = early()
	if(f > 1)
		f = f * factorial(f - 1)
	end
	return(f)
end



func counting()
	for i from 1 to 6
		print(i)
		if(i > 3)
			continue()
		end
		print("SKIPPED THE CONTINUE")
	end
end